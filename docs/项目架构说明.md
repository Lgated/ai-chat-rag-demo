# AI 聊天室项目 - 架构说明

## 📐 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (React)                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  聊天界面    │  │  会话列表    │  │  消息展示    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓ HTTP/SSE
┌─────────────────────────────────────────────────────────────┐
│                      API 层 (Controller)                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ChatController│ │RagController │ │AgentController│      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     业务层 (Service)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ ChatService  │  │  RagService  │  │ AgentService │      │
│  │ AiService    │  │EmbeddingService│ │  Tool       │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     数据层 (Repository)                       │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │Conversation  │  │  Message     │  │DocumentChunk │      │
│  │ Repository   │  │  Repository  │  │ Repository   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     数据存储层                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ PostgreSQL   │  │  pgvector    │  │   Neo4j      │      │
│  │ (业务数据)    │  │ (向量检索)    │  │ (知识图谱)    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     外部服务                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ DashScope    │  │  Embedding   │  │  其他 API    │      │
│  │ (大模型)      │  │  (向量化)     │  │  (工具调用)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 数据流图

### 普通对话流程

```
用户输入
   ↓
前端发送 HTTP POST
   ↓
ChatController.addMessage()
   ↓
ChatService.addMessage()
   ├→ 保存用户消息到数据库
   ├→ 获取历史消息
   └→ AiService.generateResponse()
       ↓
   调用大模型 API (DashScope/OpenAI)
       ↓
   返回回答
   ↓
ChatService 保存 AI 回答到数据库
   ↓
Controller 返回给前端
   ↓
前端展示消息
```

### RAG 对话流程

```
用户输入问题
   ↓
前端发送请求
   ↓
RagController.ragChat()
   ↓
RagService.retrieveRelevantChunks()
   ├→ EmbeddingService.embedText() (问题向量化)
   ├→ 向量数据库检索 (pgvector)
   └→ 返回相关文档片段 (topK)
   ↓
RagService.generateRagResponse()
   ├→ 构建 Prompt (问题 + 文档片段)
   └→ AiService.generateResponse()
       ↓
   调用大模型
       ↓
   返回基于文档的回答
   ↓
保存消息并返回前端
```

### SSE 流式输出流程

```
用户输入
   ↓
前端发送 GET 请求 (SSE)
   ↓
ChatController.streamChat()
   ↓
ChatService.streamAiResponse()
   ├→ 保存用户消息
   └→ AiService.streamResponse()
       ↓
   调用大模型流式 API
       ↓
   返回 Flux<String> (流)
       ↓
Controller 转换为 Flux<ServerSentEvent>
       ↓
前端 EventSource 接收
   ├→ 逐字显示
   └→ 流结束后保存完整回答
```

---

## 📦 模块职责说明

### 1. Controller 层

**职责**：
- 接收 HTTP 请求
- 参数验证
- 调用 Service 层
- 返回统一格式响应

**主要类**：
- `ChatController`：会话和消息管理
- `RagController`（未来）：RAG 相关接口
- `AgentController`（未来）：Agent 相关接口

### 2. Service 层

**职责**：
- 业务逻辑处理
- 事务管理
- 调用 Repository 和外部服务

**主要接口**：
- `ChatService`：会话和消息业务
- `AiService`：大模型调用
- `RagService`：RAG 检索
- `EmbeddingService`：向量化
- `AgentService`：Agent 编排

### 3. Repository 层

**职责**：
- 数据访问
- 数据库查询

**主要接口**：
- `ConversationRepository`：会话数据访问
- `MessageRepository`：消息数据访问
- `DocumentChunkRepository`：文档向量数据访问

### 4. Domain 层

**职责**：
- 实体类定义
- 数据模型

**主要类**：
- `Conversation`：会话实体
- `Message`：消息实体
- `DocumentChunk`：文档片段实体（向量存储）

---

## 🗄️ 数据库设计

### conversation 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| title | varchar(255) | 会话标题 |
| created_at | timestamp | 创建时间 |

### message 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| conversation_id | bigint | 外键，关联 conversation |
| role | varchar(20) | 角色：user/assistant |
| content | text | 消息内容 |
| created_at | timestamp | 创建时间 |

### document_chunk 表（RAG 用）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint | 主键 |
| doc_id | bigint | 文档 ID |
| content | text | 文档内容 |
| embedding | vector(1536) | 向量（pgvector 类型） |
| created_at | timestamp | 创建时间 |

**索引建议**：
- `conversation_id` 添加索引（查询消息时使用）
- `embedding` 添加向量索引（相似度检索使用）

---

## 🔌 API 接口设计

### 基础接口

```
GET  /api/chat/conversations           # 获取所有会话
POST /api/chat/conversations           # 创建会话
GET  /api/chat/conversations/{id}      # 获取单个会话
GET  /api/chat/conversations/{id}/messages  # 获取消息列表
POST /api/chat/conversations/{id}/messages  # 发送消息
```

### SSE 流式接口

```
GET  /api/chat/conversations/{id}/stream?message=xxx  # 流式对话
```

### RAG 接口

```
POST /api/chat/conversations/{id}/rag  # RAG 对话
POST /api/rag/ingest                   # 文档入库
```

---

## 🛠️ 技术栈

### 后端
- **框架**：Spring Boot 3.3.0
- **Web**：Spring WebFlux (响应式)
- **数据**：Spring Data JPA
- **数据库**：PostgreSQL 15+
- **向量**：pgvector (PostgreSQL 扩展)
- **AI**：Spring AI (或 DashScope SDK)
- **构建**：Maven

### 前端
- **框架**：React 18+
- **语言**：TypeScript
- **HTTP**：Axios
- **构建**：Vite

---

## 📋 配置文件说明

### application.yml

```yaml
server:
  port: 8080  # 服务端口

spring:
  application:
    name: test-easyjava
  
  datasource:
    url: jdbc:postgresql://localhost:5432/ai_chat
    username: postgres
    password: 123456
  
  jpa:
    hibernate:
      ddl-auto: update  # 自动建表
    show-sql: true      # 显示 SQL
  
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}  # 从环境变量读取
```

---

## 🔐 安全注意事项

1. **API Key 管理**
   - 使用环境变量存储
   - 不要提交到 Git
   - 生产环境使用配置中心

2. **CORS 配置**
   - 前端和后端分离时需要配置跨域
   - 开发环境可以允许所有来源
   - 生产环境限制具体域名

3. **输入验证**
   - Controller 层验证参数
   - 防止 SQL 注入（使用 JPA 参数化查询）
   - 防止 XSS（前端转义）

---

## 📈 性能优化建议

1. **数据库优化**
   - 添加必要的索引
   - 查询时只选择需要的字段
   - 使用分页查询（如果数据量大）

2. **缓存策略**
   - 会话列表可以缓存
   - Embedding 结果可以缓存（相同文本）

3. **并发处理**
   - WebFlux 天然支持高并发
   - 数据库连接池配置合理大小

4. **RAG 优化**
   - 限制检索数量（topK）
   - 使用批量 embedding（减少 API 调用）

---

## 🧪 测试策略

### 单元测试
- Service 层业务逻辑
- 工具类方法

### 集成测试
- Controller 接口
- 数据库操作

### 端到端测试
- 完整对话流程
- 前端交互

---

## 🚀 部署建议

### 开发环境
- 本地运行 PostgreSQL
- 本地运行后端
- 本地运行前端

### 生产环境
- Docker Compose 编排所有服务
- 使用 Nginx 反向代理
- 配置 HTTPS
- 使用环境变量管理配置

---

**架构说明完成！如有疑问，请参考开发计划文档。**

